import React from "react";
import { AddTagButton } from "..";

interface AddTagButtonContainerProps {}

const AddTagButtonContainer: React.FC<AddTagButtonContainerProps> = ({}) => {
  const handleFetchList = () => {
    const tags = true;
    if (!tags) {
    }
  };

  return <AddTagButton />;
};

export default AddTagButtonContainer;
