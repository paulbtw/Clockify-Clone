import * as types from '../constants/ActionTypes';
import { actionStart, actionFailed, actionSuccess } from './utils/template';

export const stop = (uid: string, text: string, date: Date, tagId: string) => (dispatch: any) => {};

export const start = (uid: string, text: string, date: Date, tagId: string) => (dispatch: any) => {};
